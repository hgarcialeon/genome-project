genomeVersion: 0.1

company:
  name: Acme Billing

departments:
  operations:
    agents:
      coordinator:
        role: Operations Coordinator
        autonomy: supervised

policies:
  after-hours-change:
    requiresApprovalFrom:
      - operations.coordinator
